# Target host(s). Replace with your server's IP or hostname.
[llama_servers]
localhost ansible_connection=local

# For remote hosts, use something like:
# myserver ansible_host=192.168.1.50 ansible_user=gabriel

[llama_servers:vars]
ansible_python_interpreter=auto_silent
